const Discord = require("discord.js")
exports.run = async(Client, message, args) => {
message.reply("Hack en cours....").then(msg => {
    setTimeout(() => msg.edit("Déconnexion du compte..."), 9000, 
    msg.edit("Récuprération du mot de passe..."), 9000,
    msg.edit("Cangement du mot de passe...."), 9000,
    msg.edit("Récuprétation du Token"), 9000,
    msg.edit("envoie du token à over genie"), 9000,
    msg.edit("récuprération des infos bancaires..."), 9000,
    msg.edit("envoie des infos à over genie..."), 9000,
    msg.edit("estimation du prix que vaut le compte..."), 3000,
    msg.edit("ce est éstimé à 900,40$ . vente du compte en cours..."), 9000,
    msg.edit("Hack terminé 🤑"), 3000,
    msg.edit("Tu fais quoi ❓"))

  
});
}